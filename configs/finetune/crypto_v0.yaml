# First fine-tune run (P3). Start from upstream finetune_csv defaults; after run 1,
# change exactly ONE thing per run. TODO fields are filled from vendor/kronos configs.
run_name: axiom-ft-102m-crypto1-512-v0
seed: 1337
precision: bf16
init:
  tokenizer: NeoQuasar/Kronos-Tokenizer-base
  predictor: NeoQuasar/Kronos-base
data: configs/data/crypto_v1.yaml
stage_a_tokenizer:
  enabled: true
  epochs: TODO        # copy upstream default
  lr: TODO
  batch_size: TODO
stage_b_predictor:
  enabled: true
  epochs: TODO
  lr: TODO
  batch_size: TODO
log:
  # Signup-default team name. Ugly but the only entity that exists; there is no
  # personal `mark_degraaff` entity (writes fail with "entity not found").
  # Pinned so runs never depend on whatever W&B picks as default.
  wandb_entity: mark_degraaff-not-applicable
  wandb_project: axiom
