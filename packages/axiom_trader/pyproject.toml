[project]
name = "axiom-trader"
version = "0.0.1"
description = "Axiom trader package"
requires-python = ">=3.11"
dependencies = ["pydantic", "pyyaml"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
